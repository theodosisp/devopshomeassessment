server {
    listen 80;
    server_name _;

    # Assessment / ALB health check (Part 4+)
    location = /healthz {
        default_type text/plain;
        return 200 'ok';
    }

    location / {
        default_type text/plain;
        return 200 'devops-assessment demo';
    }
}
